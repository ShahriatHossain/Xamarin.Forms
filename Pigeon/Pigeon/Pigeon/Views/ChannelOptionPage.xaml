<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Pigeon.Views.ChannelOptionPage"
             NavigationPage.HasBackButton="True"
             NavigationPage.BackButtonTitle=""
             Title="PIGEON">
  <!--<ContentPage.ToolbarItems>
    <ToolbarItem Name="Back" Command="{Binding GotoParentCommand}" Order="Primary" Priority="0">
      <ToolbarItem.Icon>
        <OnPlatform x:TypeArguments="FileImageSource" iOS="back_icon.png" Android="back_icon.png" />
      </ToolbarItem.Icon>
    </ToolbarItem>
  </ContentPage.ToolbarItems>-->
  <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
    <Button Text="Channel Details" Command="{Binding NavigateToChannelDetailsPageCommand}" WidthRequest="300" Style="{StaticResource buttonStyleRed}" />
    <Button Text="Secure ID" Command="{Binding NavigateToSecureIdPageCommand}" WidthRequest="300" Style="{StaticResource buttonStyleRed}" Margin="0,50,0,0" />
  </StackLayout>
</ContentPage>